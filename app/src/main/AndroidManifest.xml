<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miniprojectbook">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchBook"></activity>
        <activity android:name=".ViewAllBook" />
        <activity android:name=".ViewProfile" />
        <activity android:name=".StudentLoggedIn" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".StudentLogin" />
        <activity
            android:name=".AdminLoggedIn"
            android:label="@string/title_activity_admin_logged_in" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>